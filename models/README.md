# Model checkpoint

`lr_nestedcv_final_C0.01.joblib` — logistic regression classifier on Perch v2 embeddings, trained with nested cross-validation (`C=0.01`).

Same checkpoint as `runs/year_pipeline_1200x600/perch_standalone` in the thesis repository.

Contents: `classifier`, `label_encoder`, `scaler`, `metadata`.
